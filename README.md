# Pride Flags

a few hand written SVGs of pride flags :3

You can find the SVGs in the `./svg` folder.

## Generate PNGs from the SVGs

You can use mogrify like the folowing

```bash
mogrify -format jpg *
```
